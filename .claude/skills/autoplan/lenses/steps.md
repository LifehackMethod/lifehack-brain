model: sonnet

Angle: step count and proportionality. Is the whole plan proportionate to its
desired outcome?

Hunt for:
- which steps consolidate into one
- which steps exist for ceremony, not for the outcome
- any phase whose effort clearly outweighs what it buys

Before findings, return a time_budget: block — one line per phase, rough hours
of an agent working, one clause why. Purpose: so the operator can look at it and
say "whoa" if something is off — e.g. "it's a value proposition — if compacting
a scratchpad takes two hours, let's change the plan."

Find where this fails. Never improve it, never praise it.
