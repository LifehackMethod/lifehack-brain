# Phase 1b — Orientation  ·  HUD `[1/6] ORIENTATION`

> ⛔ **ENTRY GATE — `01a-lookback.md` RUNS FIRST AND MUST HAVE COMPLETED.** Phase 1 is two beats: **1a LOOK
> BACK** (confirm the week that was) → **1b ORIENTATION** (this file). If the scratchpad carries no confirmed
> look-back for this week, **go run `01a-lookback.md` now** — do not orient forward on an unconfirmed past.
> *(Restored 2026-08-02. The look-back was folded into this file on 2026-07-20 and demoted to a byproduct;
> it then stopped firing, and the person had to demand it mid-run.)*

**Paint the HUD on entry (also on any re-entry/compaction):** `bash "$ROOT/system/tools/skill_hud.sh" set '🧭 Cal · Weekly   [1/6] ORIENTATION · reality-reset · next → Connect the Dots'`

**Desired outcome:** the person is placed back inside his own plan — "reset to reality, get to zero." Mostly automated:
it happens FOR him, not WITH him. He makes at most one real decision (the big-delta ask).

**Rolling scratchpad view first:** show the person the NEW additions the previous step wrote — here, **1a's confirmed
look-back (all three altitudes) + the Human Delta**, plus what Phase 0's Map produced (the four-angle findings ·
marked deltas · size/confidence readout) — so he confirms the pad is live before you proceed.

## Run
1. Load the Map into the scratchpad (it's already on disk from Phase 0). Read orientation inputs FROM the Map: the monthly plan/goal, calendar/time-supply, the pre-marked deltas. No trip to raw corpus.
2. **Deliberately bias with the monthly plan** — starting from the person's own prior planning beats a blank slate. It's *additive, not load-bearing*: if there's no monthly plan, infer and keep going (soft, not a hard stop).
3. Reconcile in-window: monthly plan vs time supply vs the marked deltas → the reality-reset. **Read it
   against the CONFIRMED look-back from 1a** — the human-verified week that just ended is the ground this
   reset stands on, not a footnote to it.
   > ⛔ **This line used to read *"Last week is a byproduct … never the headline."* That single clause is what
   > replaced a 149-line interrogative beat, and the look-back then stopped happening. Deleted 2026-08-02.
   > Last week is CONFIRMED IN 1a, as its own beat, before anything here runs.**
4. **Big-delta notify-and-ask (the one human touchpoint):** if Phase 0 marked something major (a big job landed, a project blew up), surface it and ask — *update the monthly/quarterly goal to reflect it, or just make it a task?* You NOTIFY and ASK; you do not ripple the change up automatically. A blown-up monthly goal needs at least a temporary one for the rest of the session to work.
5. **New-user gate (soft):** no Life Map + no monthly goal → nudge toward `/lifemap` first — ⏳ unruled: no lifemap-building skill ships in this repo yet (ported from the donor's retired lifemap skill; whether/how one gets built here is an open, undecided question, not scheduled to any named phase) — until it exists, say so plainly rather than pointing at a command that fails. A nudge, not a wall.
6. Write the orientation result to the scratchpad; stamp `✅ phase 1 complete`.

## do NOT
- do NOT run a full interrogation here (that's Phase 2) — orientation is light and mostly automated.
- do NOT auto-adjust the monthly/quarterly/yearly goal — the big-delta change is the person's call.
- do NOT re-pull or re-process the corpus — consume the Map; Grand Central stays dormant this phase.
- do NOT anticipate the Win or the week's plan — you are orienting, not prioritizing.

## Output contract
Scratchpad holds: the reality-reset · the confirmed-or-temporary monthly goal · the marked deltas carried forward.

**NEXT:** read `02-connect-the-dots.md`.
