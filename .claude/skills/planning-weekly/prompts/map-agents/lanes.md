# Map-agent — Angle 2: LANES

**Your one angle:** life-coverage across the 10 lanes. **The lanes are EMBEDDED in your dispatch brief** (the
launching session pastes them in from `user-canon.md` — you are a blind sub-agent, so work from what's embedded;
do not try to read files or the chat). For each lane: is it getting pushed forward this week, or is it dark? Which
lanes have no signal at all in the data?

Read the week's window through the lane lens. Surface, per lane, whether it's moving / stalled / dark, with a
pointer to the evidence. A quiet lane is a SIGNAL, not missing data — flag it, don't drop it. Pre-draft one
question per lane worth probing (with your best guess).

Return in the shared Map format (`_map-format.md`). Do NOT rank lanes against each other or name a Win — coverage
mapping only.
