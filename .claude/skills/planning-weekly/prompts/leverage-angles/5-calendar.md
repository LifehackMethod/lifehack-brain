# Leverage angle 5 — CALENDAR

**Your currency:** leverage already embedded in scheduled time. Where in the week's actual calendar is leverage
already sitting — a meeting, a block, a window that, used well, returns far more than its hours? The move that
exploits time already committed.

Find the candidate Win embedded in the existing calendar. Return in `_angle-format.md`. One candidate only.
